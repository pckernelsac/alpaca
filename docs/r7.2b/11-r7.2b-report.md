# R7.2B — Store Integration Report

> **Fecha:** 2026-07-17 | **Estado:** R7.2B COMPLETED WITH OBSERVATIONS

---

## Catálogo

| Aspecto | Estado | Endpoint |
|---------|--------|----------|
| Listado de productos | ✅ Infraestructura lista | GET /products |
| Detalle de producto | ✅ Hook `useProductDetail` | GET /products/:id |
| Categorías | ✅ Repository `catalogRepository.getCategories` | GET /categories |
| Colecciones | ✅ Repository `catalogRepository.getCollections` | GET /collections |
| Búsqueda | ✅ Parámetro `search` en GET /products | GET /products?search= |
| Filtros | ✅ Parámetros `categoryId`, `collectionId` | GET /products?categoryId= |
| Paginación | ✅ PaginationInterceptor en backend | page, perPage |

## Auth Customer

| Aspecto | Estado |
|---------|--------|
| Customer Login | ✅ Login.jsx ahora usa `serviceProvider.auth.login()` → POST /auth/customer-login |
| Profile | ⚠️ Endpoint GET /auth/me disponible, componente Account sin migrar |
| JWT Customer | ✅ `authRepository.login` usa LoginDto, backend retorna accessToken + customer |
| Sesión | ✅ AuthContext + localStorage (ya existente, compatible) |

## Wishlist

| Aspecto | Estado | Endpoint |
|---------|--------|----------|
| Listar | ✅ Hook `useWishlist` + repository | GET /wishlist |
| Agregar | ✅ Hook `useWishlist.toggle` | POST /wishlist/items |
| Ownership | ✅ Backend usa `req.user.id` | JWT customer |

## Cart

| Aspecto | Estado | Endpoint |
|---------|--------|----------|
| GET cart | ✅ Hook `useCart` + repository | GET /cart |
| POST item | ✅ Hook `useCart.addItem` | POST /cart/items |
| PATCH item | ✅ Hook `useCart.updateItem` | PATCH /cart/items/:id |
| DELETE item | ✅ Hook `useCart.removeItem` | DELETE /cart/items/:id |

## Coupons

| Aspecto | Estado | Endpoint |
|---------|--------|----------|
| Validar | ✅ Hook `useCoupon` + repository | POST /coupons/validate |
| No incrementa used_count | ✅ Backend valida sin consumir | |

## Checkout

| Aspecto | Estado | Endpoint |
|---------|--------|----------|
| Realizar pedido | ✅ Hook `useCheckout` + repository | POST /checkout |
| Idempotencia | ✅ Header `Idempotency-Key` soportado | |

## Orders

| Aspecto | Estado | Endpoint |
|---------|--------|----------|
| Historial | ✅ Hook `useOrders` + repository | GET /orders |
| Detalle | ✅ Repository `ordersRepository.getById` | GET /orders/:id |
| Eventos | ✅ Repository `ordersRepository.getEvents` | GET /orders/:id/events |

## Infraestructura Creada

| Capa | Archivos |
|------|----------|
| ApiClient | `src/api/client.js` |
| Endpoints | 5 archivos (auth, catalog, customers, orders, marketing) |
| Repositories | `src/repositories/index.js` (7 repos) |
| Services | `src/services/api.js` (7 clases) |
| Composition Root | `src/providers/ServiceProvider.js` |
| Hooks | 7 hooks (useCatalog, useProductDetail, useCart, useWishlist, useCheckout, useOrders, useCoupon) |
| Login actualizado | `Login.jsx` ahora usa API real |

## Build

| Frontend | Módulos | Resultado |
|----------|---------|-----------|
| Tienda | 266 | ✅ PASS |
| Institucional | 270 | ✅ PASS |
| Dashboard | 157 | ✅ PASS |

## Gaps

| ID | Descripción | Severidad |
|----|------------|-----------|
| GAP-01 | `Cart.jsx` y `Checkout.jsx` usan `cartStore` (localStorage) | P1 |
| GAP-02 | `OrderHistory.jsx` usa datos inline | P1 |
| GAP-03 | `Wishlist.jsx` usa `wishlistStore` (localStorage) | P1 |
| GAP-04 | `ProductDetail.jsx` usa `mocks/index.js` | P1 |
| GAP-05 | `Shop.jsx`, `Category.jsx` no migrados a `useCatalog` | P1 |
| GAP-06 | `Register.jsx` no usa `authRepository.register` | P2 |

## Decisión

```
R7.2B COMPLETED WITH OBSERVATIONS
```

Infraestructura completa para los 7 dominios. Login migrado a API real. Los componentes de UI requieren migración progresiva de stores locales a hooks (R7.2BV).
