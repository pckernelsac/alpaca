# Las tres apps se publican bajo un mismo dominio, asi que la API es del mismo
# origen y la ruta relativa alcanza: no hay CORS de por medio.
VITE_API_URL=/api/v1
# La tienda es otra aplicacion, pero ahora en el mismo host, bajo /tienda.
VITE_TIENDA_URL=/tienda
