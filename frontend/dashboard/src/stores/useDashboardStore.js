import { create } from 'zustand'; import dashboardRepo from '../repositories/dashboard.js'; const useDashboardStore = create((set) => ({ kpis: null, recentOrders: [], alerts: [], loading: false, fetchKpis: async () => { set({ loading: true }); const kpis = await dashboardRepo.kpis(); const recentOrders = await dashboardRepo.recentOrders(); const alerts = await dashboardRepo.alerts(); set({ kpis, recentOrders, alerts, loading: false }); }, })); export default useDashboardStore;
