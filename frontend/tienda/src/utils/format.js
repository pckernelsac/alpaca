export function formatCurrency(value) { return new Intl.NumberFormat('es-PE', { style: 'currency', currency: 'PEN' }).format(value); } export function formatDate(date) { return new Intl.DateTimeFormat('es-PE').format(new Date(date)); } export function formatPercentage(value) { return value + "%"; }